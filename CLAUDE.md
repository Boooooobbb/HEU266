# Claude 全局设置

## 语言
- 始终用中文回复。

## 分支策略
- 每开始一个新功能或修复任务时，主动询问我是否需要创建专用分支。
- 如果我直接让你开始开发，自动执行 `git checkout -b feature/ai-<简短描述>` 创建并切换到该分支。
- 所有 git 提交、推送操作仅限当前分支，**绝对不要**自动合并到 main 或 master。
- `git push` 等远程操作必须在执行前征得我明确同意。

## 通用规则
- 提交信息使用英文，格式为：`type(scope): description`。
- 修改代码后，主动建议运行相关的测试或检查。
- 不确定的设计决定先与我讨论，不要自行假设。